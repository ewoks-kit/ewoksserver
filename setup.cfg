[metadata]
name = backend

[options]
packages = find:
python_requires = >=3.6
install_requires = 
    flask
    flask-restful
    flask-cors

[options.entry_points]
console_scripts =
    ewoks-server=backend.app.server:main
